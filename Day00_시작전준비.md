# Day 0 — Claude Desktop의 Code 처음 준비하기

오늘은 수업이 아니라 **개강 전 설치 점검일**입니다. 마지막에 `ai-challenge` 폴더를 Claude Desktop의 Code에서 열고, `hello.txt` 안에 `첫 파일 만들기 성공`이 보이면 끝입니다.

오늘 누를 순서는 이것뿐입니다.

```text
공식 사이트에서 설치 → 로그인 → 홈 채팅으로 첫 답장 받기
→ 바탕 화면에 ai-challenge 폴더 만들기 → Code → 로컬 → 폴더 선택
→ 수동(Ask permissions) → hello.txt 만들기 → 파일 내용 확인
```

Day 1~7 자료는 오늘 받지 않아도 됩니다. 설치와 첫 파일까지 성공한 뒤 Day 1에서 필요한 자료를 따로 받습니다.

> 이 문서의 버튼 이름은 앱 버전과 언어 설정에 따라 조금 다를 수 있습니다. 같은 단어가 없으면 비슷한 뜻의 버튼을 찾고, 5분 넘게 막히면 화면을 찍어 질문하세요.

## 시작하기 전에 보는 실제 화면

### 1. 왼쪽 위 `홈`과 `Code`

![Claude Desktop의 홈과 Code 위치](day00-assets/screenshots/01_home_code.png)

- **홈:** 질문·글쓰기·아티팩트(Artifacts)·프로젝트(Projects)·코워크(Cowork)처럼 대화 중심 작업을 시작하는 곳
- **Code:** 내가 선택한 컴퓨터 폴더의 파일을 만들고 고치며 Preview와 Diff를 보는 곳
- 오늘 첫 인사는 **홈**, `hello.txt` 만들기는 **Code**에서 한다.

### 2. 홈 안의 `채팅`과 `Cowork`

![홈 입력창의 채팅, Cowork, 모델, Effort, 폴더와 권한 위치](day00-assets/screenshots/02_cowork_controls.png)

- 입력칸 **왼쪽 아래 `채팅 / Cowork`** — 답을 받을지, 여러 단계 작업을 맡길지 선택
- 입력칸 **아래 줄 `프로젝트 또는 폴더`** — Claude에게 보여 줄 자료 범위 선택
- 그 오른쪽 **`수동`** — **Cowork가** 파일·앱을 사용하기 전에 사람이 승인
- 입력칸 **오른쪽 `Opus 5`** — 캡처 당시 선택된 모델 이름
- 모델 이름 옆 **`높음`** — Effort, Claude가 작업을 얼마나 깊게 수행할지 선택

여기 보이는 코워크의 `수동`과 아래 Code의 `수동`은 서로 다른 메뉴다. 둘 다 사람이 확인한다는 원칙은 같지만, 코워크는 앱·파일 작업을, Code는 프로젝트 파일 편집·명령 실행을 다룬다.

### 3. Code에서 작업 시작 전 정하는 네 가지

![Code 입력창의 로컬, 폴더, 권한, 모델과 작업 강도](day00-assets/screenshots/03_code_controls.png)

- 입력칸 **위 왼쪽 `로컬(Local)`** — 지금 이 컴퓨터에서 실행
- 그 옆 **`폴더 선택…`** — 오늘 연습 폴더 하나만 허용
- 입력칸 **아래 왼쪽 권한 메뉴** — 오늘 첫 실습은 `수동(Ask permissions)`, 익숙해지면 `자동`, 큰 작업은 먼저 `계획(Plan)`
- 입력칸 **아래 오른쪽 모델·Effort** — 아래 수강 기본값으로 통일

> 이 캡처에는 촬영 당시 `자동`이 보인다. **오늘 첫 실습만 `자동`을 눌러 `수동`으로 바꾼다.** Claude가 내 파일을 어떻게 바꾸는지 한 번은 직접 봐야 하기 때문이고, 그걸 본 뒤에는 자동이 더 편하다.

## 왕초보 챌린지 고정 설정

수업 중에는 선택 때문에 시간을 쓰지 않도록 아래 값으로 시작한다.

| 항목 | 수강 기본값 | 이유 |
|---|---|---|
| 실행 위치 | **로컬(Local)** | 제공 폴더와 내 컴퓨터에서 바로 결과 확인 |
| 작업 폴더 | **그날 받은 Day 폴더 하나** | 다른 개인 파일이 섞이지 않게 제한 |
| 모델 | **화면에 처음 선택된 기본 모델 유지** | 계정·요금제마다 보이는 모델이 달라도 함께 시작 가능 |
| 작업 강도 | **높음이 보이면 높음, 없으면 기본값** | 오늘 과제를 꼼꼼하게 살펴보는 정도 |
| 권한 | **오늘은 수동, 내일부터 자동 가능** | 뭘 바꾸는지 오늘 한 번 직접 본 뒤에는 자동이 편하다 |
| 큰 작업 첫 단계 | **계획(Plan)** | 파일을 고치기 전에 계획부터 확인 |

`Effort`는 모델 이름이 아니라 **이번 작업을 얼마나 깊게 살펴볼지** 정하는 값이다. 화면에 **높음**이 보이면 고르고, 메뉴가 없거나 다른 선택지만 보이면 처음 선택된 기본값을 유지한다. 없는 선택지를 찾느라 멈추지 않는다.

큰 작업은 다음 네 번만 누르면 된다.

1. 권한 모드 메뉴 → **계획(Plan)**
2. 요청 보내기 → Claude가 제안한 작업 순서 읽기
3. 바꿀 파일과 결과물이 맞으면 권한 모드 메뉴 → **수동(Ask permissions)**
4. `이 계획대로 실행해 주세요`라고 보내고 변경 비교(Diff)를 확인한 뒤 승인

계획에서는 계획만 만들고 파일은 아직 바뀌지 않는다. 실행을 시작하기 전 수동으로 돌아오는 것이 수강 규칙이다. 오늘은 이 순서를 읽기만 하고, 실제 `hello.txt` 만들기는 처음부터 **수동**으로 진행합니다.

## 준비물

- Windows PC 또는 Mac 1대
- 자신이 계속 사용할 이메일 계정
- 휴대폰
- 유료 계획이 필요한 경우 결제 수단

휴대폰만으로는 참여하기 어렵습니다. 공용 PC와 회사 PC는 파일 저장·앱 설치가 막힐 수 있어 권하지 않습니다.

## 가장 중요한 규칙: 5분 막힘

같은 화면에서 5분 이상 막히면 추측하여 계속 누르지 않습니다.

1. 화면에 카드번호, 비밀번호, 인증번호, API 키가 보이는지 먼저 봅니다.
2. 보인다면 그 부분을 가리고 휴대폰으로 화면을 찍습니다.
3. 참가자방에 `Day 0 / 막힌 단계 / 화면 사진`을 올립니다.
4. 답을 기다리는 동안 결제나 삭제 버튼을 추가로 누르지 않습니다.

## 1. 클로드 데스크탑 앱 설치

1. Chrome, Edge 또는 Safari를 엽니다.
2. 주소창에 `claude.com/download`를 입력하고 Enter를 누릅니다.
3. 자신의 컴퓨터에 맞는 **Windows** 또는 **macOS** 다운로드를 고릅니다.
4. 다운로드가 끝날 때까지 기다립니다.

### Mac 설치

1. Finder의 **다운로드**에서 이름에 `Claude`가 들어간 `.dmg` 파일을 두 번 누릅니다.
2. 열린 창에서 **Claude 아이콘을 Applications(응용 프로그램) 폴더로 끌어 놓습니다.**
3. 복사가 끝나면 Finder 왼쪽 **응용 프로그램**을 열고 **Claude**를 두 번 누릅니다.
4. “인터넷에서 다운로드한 앱” 확인창이 나오면, 주소를 `claude.com/download`에서 받았는지 다시 본 뒤 **열기**를 누릅니다.
5. 차단됐다는 화면이 나오면 임의 파일을 다시 받지 말고 `시스템 설정 → 개인정보 보호 및 보안 → 확인 없이 열기`를 찾습니다. 공식 사이트에서 받은 파일일 때만 진행합니다.

### Windows 설치

1. 파일 탐색기의 **다운로드**에서 이름에 `Claude`가 들어간 설치 파일을 두 번 누릅니다.
2. Windows가 실행 허용을 물으면 앱 이름과 게시자가 Claude/Anthropic인지 확인한 뒤 **예**를 누릅니다.
3. 설치가 끝날 때까지 창을 닫지 않습니다.
4. 시작 메뉴를 열고 `Claude`를 검색해 **Claude** 앱을 누릅니다.
5. Windows 보호 화면이 뜨거나 게시자를 확인할 수 없으면 실행을 멈추고 화면을 운영자에게 보냅니다.

### Windows만 한 번 더 확인

Claude Desktop의 Code를 처음 쓰려면 **Git for Windows**가 필요할 수 있습니다. Code가 열리지 않거나 Git 설치 안내가 나오면 [Git for Windows 공식 다운로드](https://git-scm.com/download/win)에서 설치한 뒤 Claude 앱을 완전히 종료하고 다시 엽니다. 설치 중 잘 모르는 추가 선택지는 기본값을 유지합니다.

**성공 화면:** Claude 앱에 로그인 안내 또는 대화 화면이 보입니다.

### Day 7을 위한 별도 사전 설치

Day 7의 연습 MP4 조립에는 Python과 FFmpeg가 필요합니다. 오늘 Claude 기본 설치가 끝난 뒤, 수업 전 별도 시간에 운영자와 [Day 7 사전 설치 클리닉](day07-assets/INSTALL_CLINIC.md)을 진행합니다. 수강생 혼자 임의 설치 명령을 실행하지 않습니다.

## 2. 로그인과 대화 확인

1. **Sign in** 또는 **로그인**을 누릅니다.
2. 계속 사용할 계정 하나로 로그인합니다.
3. 대화 입력칸에 `안녕하세요. 오늘 처음 설치했어요.`라고 입력합니다.
4. Enter를 눌러 보냅니다.

**성공 화면:** 클로드가 답장합니다.

로그인에 사용한 이메일 주소는 본인의 메모장에만 적습니다. 인증 사진에는 이메일 주소가 보이지 않게 합니다.

## 3. 요금제 확인

**7일 전 과정을 그대로 따라가는 개인 수강생의 최소 기준은 Pro입니다.** Pro와 Max에서는 Code와 코워크를 사용할 수 있고, 현재 베타인 디자인과 발송(Dispatch)도 사용할 수 있습니다. Max는 사용량이 훨씬 많은 사람용이므로 왕초보 2기 때문에 처음부터 Max를 선택할 필요는 없습니다.

발송은 Claude Desktop의 **Cowork 왼쪽 메뉴 → 발송**에서 엽니다. 휴대폰에서도 **Cowork → 발송**으로 들어갑니다.

Free 계정은 채팅(Chat)은 가능하지만 Code·코워크가 없어 이 과정 전체를 따라갈 수 없습니다. Team·Enterprise 계정은 Code·코워크·디자인을 쓸 수 있지만 조직 관리자가 기능을 꺼 둘 수 있고 발송은 제공되지 않으므로 Day 7에서 로컬 대체 실습을 합니다. 기능과 가격은 바뀔 수 있으므로 결제 화면의 현재 내용을 최종 기준으로 봅니다.

1. 앱의 **Settings(설정)** 또는 Claude 웹사이트의 요금제 페이지를 엽니다.
2. 개인 계정이라면 **Pro 또는 Max**, 처음 시작이면 **Pro**가 선택되어 있는지 확인합니다.
3. 결제하기 전 구독 주기와 총 결제 금액을 다시 봅니다.
4. 결제 화면은 촬영하거나 참가자방에 올리지 않습니다.

5. 결제 뒤 왼쪽 위에 **Code**, 홈 입력창에 **Cowork**, 왼쪽 메뉴에 **디자인**이 보이는지 확인합니다. 디자인이 보이지 않으면 Day 3 전에 운영자에게 화면을 보내고, 임의로 추가 결제하지 않습니다.

> 결제할 때는 `Pro 이상 / 월간·연간은 본인 선택 / 자동 갱신일 확인` 세 가지만 기억합니다. 확신이 없으면 결제하지 말고 운영자에게 화면을 보여 줍니다.

## 4. 수업 폴더 만들기

### Windows

1. 바탕 화면의 빈 곳에서 마우스 오른쪽 버튼을 누릅니다.
2. **새로 만들기**를 누릅니다.
3. **폴더**를 누릅니다.
4. 이름을 `ai-challenge`로 바꾸고 Enter를 누릅니다.

### Mac

1. 데스크탑의 빈 곳을 한 번 누릅니다.
2. 화면 맨 위의 **파일**을 누릅니다.
3. **새로운 폴더**를 누릅니다.
4. 이름을 `ai-challenge`로 바꾸고 Return를 누릅니다.

**성공 화면:** 바탕 화면/데스크탑에 `ai-challenge` 폴더가 보입니다.

## 5. Claude Code에서 폴더 열기

1. Claude 앱에서 **Code**를 찾아 누릅니다.
2. 내 컴퓨터에서 작업하는 선택지가 보이면 **로컬(Local)을** 고릅니다.
3. **폴더 선택…을** 누릅니다.
4. 방금 만든 `ai-challenge` 폴더를 고릅니다.
5. 모델은 화면의 기본값을 유지하고, 작업 강도(Effort)는 **높음(High)이 보이면 높음**, 메뉴가 없으면 기본값으로 둡니다.
6. 권한 선택이 보이면 **수동(Ask permissions)을** 고릅니다.
7. 입력칸에 `지금 열린 폴더 이름을 알려 주고, hello.txt 파일을 만든 다음 파일 안에 첫 파일 만들기 성공이라고 적어 줘.`라고 입력합니다.
8. 파일 생성 허용을 물으면, 대상이 `ai-challenge` 안의 `hello.txt`인지 보고 허용합니다.
9. 왼쪽 파일 목록에서 `hello.txt`를 한 번 눌러 엽니다.

**성공 화면:** 파일 목록에 `hello.txt`가 보이고, 파일을 열었을 때 `첫 파일 만들기 성공`이 보이며, 클로드가 완료했다고 답합니다.

> **이제 자동으로 바꿔도 됩니다.** 방금 승인 창에서 Claude가 어떤 파일을 만드는지 직접 확인했습니다 — 그 감각을 얻는 것이 오늘 수동의 목적이었습니다. 내일부터는 권한 메뉴에서 **자동**을 골라도 됩니다. 자동에서도 위험한 작업은 먼저 물어봅니다. 코워크(Day 4)와 코드(Day 5) 수업에서만 승인 흐름을 배우기 위해 그날 다시 수동으로 바꾸라고 안내합니다.

버튼 이름이 다르면 먼저 앱을 최신 버전으로 업데이트합니다. 이 교재는 현재 한국어 UI의 **수동**을 기준으로 합니다.

## 최종 인증 — 30초

참가자방 링크와 제출 기한은 **별도 개강 공지**를 따릅니다. 공개 교재에는 입장 링크를 넣지 않습니다.

📸 **인증 캡처:** `hello.txt`가 열려 있는 Claude Code 화면 1장
(이메일·실명이 보이면 가리고, 결제 화면은 올리지 않습니다)

```text
[왕초보2기 Day 0 / 닉네임]
✅ 설치부터 hello.txt까지 완료
💬 소감:
```

## 마지막 확인

- [ ] Claude 앱이 열린다.
- [ ] 대화에 답장이 온다.
- [ ] 바탕 화면/데스크탑에 `ai-challenge` 폴더가 있다.
- [ ] Code에서 폴더를 열었다.
- [ ] `hello.txt`가 만들어졌다.
- [ ] 개인정보를 가린 인증 사진을 준비했다.

여섯 개가 모두 체크되면 Day 1을 시작할 준비가 된 것입니다.

## 공식 확인 링크

- [Claude Desktop 설치와 요금제별 Code·Cowork 제공 범위](https://support.claude.com/en/articles/10065433-install-claude-desktop)
- [코워크 시작 방법과 수동 모드](https://support.claude.com/en/articles/13345190-get-started-with-claude-cowork)
- [Claude 디자인 시작하기](https://support.claude.com/en/articles/14604416-get-started-with-claude-design)
- [발송(Dispatch)의 Pro·Max 요구 사항](https://claude.com/docs/cowork/guide/dispatch)
