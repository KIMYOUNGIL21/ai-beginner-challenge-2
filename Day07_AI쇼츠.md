# Day 7 — AI 쇼츠 파이프라인 완주

> **현재는 운영자 검토용 초안입니다.** 수강생용 샘플 영상·음성 폴더와 기준 MP4의 새 컴퓨터 검증이 끝난 뒤 최종 공개합니다. 지금은 전체 수업 흐름을 확인하는 용도로 봐 주세요.

## 오늘의 결과물

오늘은 Claude Code에서 대본과 영상 제작 규칙을 다루고, 제공된 영상·음성으로 세로 쇼츠 한 편을 조립한다.

오픈카톡 인증물은 두 파일이다.

- `D7_닉네임_쇼츠.mp4` — 1080×1920 세로 MP4
- `D7_닉네임_썸네일.png` — 영상 내용을 대표하는 세로 이미지

기본 완주는 API 키, 유료 크레딧, Google Flow 계정 없이 가능해야 한다. 수강생은 제공받은 샘플 클립과 샘플 음성을 사용한다. 자기 소재를 Flow와 Typecast로 새로 만드는 과정은 심화다.

처음 보는 말:

- **파이프라인**: 재료가 여러 단계를 지나 완성품이 되는 작업 순서다.
- **클립**: 쇼츠에 들어갈 짧은 영상 조각이다.
- **내레이션**: 영상에 깔리는 말소리다.
- **타임스탬프**: 각 단어가 몇 초에 들리는지 적은 시간표다.
- **FFmpeg**: 영상·음성·자막을 한 파일로 합치는 프로그램이다.
- **9:16**: 휴대폰을 세로로 세웠을 때 꽉 차는 화면 비율이다.

### 기본 완주와 심화

**기본 완주**는 운영자가 제공한 `day07-start` 폴더로 샘플 쇼츠를 조립하고 썸네일을 만드는 것이다. API 키와 유료 계정은 필요 없다.

**심화**는 본인 계정으로 Flow 영상과 Typecast 음성을 새로 만들어 자기 주제의 쇼츠를 완성하는 것이다. 계정·비용·사용 권한을 확인한 사람만 진행한다.

## 네 블록

### 1. 보기 — 파이프라인 이해 20분

```text
주제
  → 반전 구조 대본
  → 씬별 Flow 프롬프트
  → 세로 영상 클립
  → 내레이션과 단어별 시간표
  → FFmpeg로 영상·음성·자막 조립
  → 9:16 MP4
```

Claude Code가 잘하는 일은 대본·프롬프트·파일 구조·검사·조립이다. Google Flow에서 영상을 생성하는 일은 사람이 한다. Typecast API는 음성과 단어별 타임스탬프를 만든다.

오늘 기본 과정에서는 이미 준비된 영상과 음성을 사용한다. 그래야 결제나 계정 문제와 무관하게 모든 사람이 조립 원리를 먼저 익힐 수 있다.

### 2. 따라하기 — 제공 샘플로 기준본 완성 60분

#### 2-1. 시작 폴더 열기

운영자가 나눠 준 `day07-start` 폴더를 Claude Code Desktop의 Code 탭에서 연다. 폴더에는 최소한 다음 항목이 있어야 한다.

```text
day07-start/
├── script.txt
├── prompts.md
├── scenes.json
├── clips/
│   ├── c01.mp4
│   └── ...
├── work/
│   ├── narration.wav
│   └── words.json
├── thumbnail-source.png
└── scripts/
    ├── build.py
    ├── check.py
    └── subs.py
```

`prompts.md`는 영상 생성용 설계도다. 기본 완주에서는 Flow에 넣지 않지만, 어떤 클립이 왜 들어갔는지 확인할 때 읽는다.

#### 2-2. Claude에게 설치 상태 확인시키기

Claude Code에 아래 프롬프트를 넣는다.

```text
이 폴더는 Day 7 AI 쇼츠 실습 폴더야.
파일을 수정하기 전에 먼저 읽기 전용으로 점검해 줘.

1. ffmpeg와 ffprobe가 실행되는지
2. scenes.json에 적힌 모든 영상이 있는지
3. 각 영상 길이가 use 값보다 짧지 않은지
4. work/narration.wav와 work/words.json이 있는지
5. Python에서 Pillow를 불러올 수 있는지

결과를 초보자가 이해할 수 있는 표로 보여 주고,
실패 항목이 있으면 한 번에 하나씩 복구 방법을 안내해 줘.
API 키를 요구하거나 유료 서비스를 호출하지 마.
```

#### 2-3. 클립 검사

Claude Code 화면 아래의 **Terminal**을 열고 아래 한 줄 전체를 복사해 붙여넣은 뒤 Enter를 누른다.

```bash
python3 scripts/check.py scenes.json
```

마지막에 `전체 씬 준비됨` 또는 개발본의 `N/N 준비됨`과 `build.py 실행 가능`이 보여야 다음으로 간다.

#### 2-4. 영상 조립

검사가 통과하면 아래 한 줄 전체를 복사한다. `닉네임`만 본인 오픈카톡 닉네임으로 바꾼 뒤 붙여넣고 Enter를 누른다.

```bash
python3 scripts/build.py scenes.json work/ --out D7_닉네임_쇼츠.mp4
```

Pillow가 없다는 메시지가 나오면 운영자가 제공한 복구 명령을 사용한다. 설치 명령을 임의로 바꾸지 말고, 오류 문장을 Claude Code에 그대로 붙여 넣는다.

#### 2-5. 결과 검수

Claude Code Preview 또는 macOS 재생기로 MP4를 열어 다음을 확인한다.

- 화면이 가로로 눕거나 찌그러지지 않았다.
- 내레이션이 들린다.
- 자막이 음성보다 눈에 띄게 빠르거나 늦지 않다.
- 자막이 화면 아래 UI 예상 영역과 겹치지 않는다.
- 마지막 장면과 음성이 갑자기 잘리지 않는다.

### 3. 내 것으로 바꾸기 — 최소 세 가지 변경 60분

기본 완성본을 확인한 뒤 다음 세 가지를 바꾼다. 이 단계는 **기본 완주 선택 과제**다. 시간이 부족하면 기준본 MP4와 썸네일만 제출해도 된다.

1. `script.txt`의 제목과 핵심 문장
2. 썸네일 제목·색 또는 이미지
3. 첫 장면과 마지막 장면의 순서 또는 사용 길이

단, 제공된 `narration.wav`와 대본 문장을 서로 다르게 바꾸면 자막·음성이 일치하지 않는다. 기본 과정에서는 대본을 ‘기획안’으로만 수정하고, 실제 음성·자막 내용은 제공본을 유지한다. 자기 대본과 정확히 맞는 영상은 아래 심화 과정에서 만든다.

Claude Code에 아래처럼 요청한다.

```text
현재 scenes.json과 샘플 영상을 먼저 확인해 줘.
첫 장면과 마지막 장면만 서로 바꾸었을 때 이야기 흐름이 자연스러운지 설명해 줘.
가능하다면 scenes.json 변경 계획을 먼저 보여 주고 내 확인 뒤 수정해 줘.
수정 후 check.py를 실행하고, 문제가 없을 때 새 이름으로 MP4를 만들어 줘.
원본 MP4와 샘플 파일은 덮어쓰지 마.
```

#### 썸네일 만들기

Claude는 사진을 직접 생성하는 도구가 아니다. 제공 이미지나 본인이 사용할 권리가 있는 이미지를 바탕으로 HTML/SVG 레이아웃 또는 이미지 편집 지시를 만든다.

```text
thumbnail-source.png를 분석하고 세로 쇼츠 썸네일 구성을 제안해 줘.
제목은 12자 안팎, 휴대폰에서 읽히는 큰 글자, 핵심 피사체를 가리지 않게 해 줘.
1080×1920 PNG로 내보낼 수 있는 HTML 또는 SVG를 만들고 Preview로 보여 줘.
출처가 불분명한 사진을 새로 가져오지 마.
```

### 4. 검수와 인증 — 30분

#### 제출 전 체크

- [ ] MP4가 세로 9:16이며 휴대폰에서 재생된다.
- [ ] 영상·음성·자막 세 요소가 모두 있다.
- [ ] 자막이 읽히고 주요 화면을 심하게 가리지 않는다.
- [ ] 썸네일 제목이 휴대폰 화면에서 읽힌다.
- [ ] 파일명에 닉네임이 있고 API 키·이메일·실명 등 민감정보가 없다.
- [ ] 남의 영상·음악·사진을 허락 없이 넣지 않았다.
- [ ] 터미널, 환경변수, 결제 화면을 인증 영상에 찍지 않았다.

#### 오픈카톡 인증문

```text
[왕초보 2기 Day 7 완료]
닉네임: ___
주제: ___
완주 방식: 기본 샘플 조립 / 심화 내 소재 제작
기본 결과: 세로 영상·음성·자막이 모두 재생됨
내가 바꾼 것(선택): ___
오늘 해결한 오류 1개: ___
다음에 직접 만들 주제: ___
```

MP4와 썸네일 PNG를 함께 올린다. 파일 업로드가 어려우면 운영자가 지정한 공유 위치에 올리고 접근 권한을 확인한 링크를 제출한다.

## 심화 — Flow와 Typecast로 내 소재 만들기

심화는 계정, 현재 요금제, 크레딧, 이용 가능 지역을 각자 확인한 사람만 진행한다.

### A. 네 부분으로 대본 쓰기

짧은 쇼츠도 다음 네 부분이 보여야 한다.

1. **훅** — 지금 방식의 문제를 한 문장으로 말한다.
2. **오해 깨기** — 흔한 해결책이 왜 부족한지 보여 준다.
3. **반전과 원리** — 예상과 다른 핵심 메커니즘을 설명한다.
4. **대구 엔딩** — 앞 문장을 되받아 기억에 남게 닫는다.

```text
내 주제는 [주제]이고 시청자는 [대상]이야.
45~60초 한국어 쇼츠 대본을 써 줘.

구조는 훅 → 흔한 해결책 무너뜨리기 → 반전 → 진짜 원리 → 대구 엔딩.
한 문장은 한 호흡으로 쓰고, 확인되지 않은 숫자나 사실은 넣지 마.
사실 주장이 있으면 출처 URL을 별도 목록으로 남겨 줘.
먼저 개요를 보여 주고 내가 승인하면 script.txt로 저장해 줘.
```

### B. Flow 프롬프트 만들기

씬마다 장면, 카메라 움직임, 스타일, 색조, 렌즈, 화면 내 문자 금지를 쓴다. `prompts.md`에는 생성 길이를, `scenes.json`에는 실제 사용할 길이를 기록한다.

```text
승인한 대본을 장면별로 나눠 prompts.md와 scenes.json 초안을 만들어 줘.
각 프롬프트는 장면 묘사 → 카메라 무빙 → 스타일 → 색조 → 렌즈/심도 순서로 써 줘.
화면 속 글자가 자막과 충돌하지 않도록 문자 생성 금지를 명시해 줘.
서로 이어지는 장면은 인물·공간·시점의 연속성을 구체적으로 적어 줘.
각 씬의 생성 길이와 실제 사용 길이를 혼동하지 않게 표로 먼저 검토시켜 줘.
```

Flow에서는 C1부터 순서대로 만들고 내려받는다. 생성 결과에 제3자의 얼굴, 상표, 캐릭터, 작품이 우연히 들어갔다면 공개 전에 교체한다.

### C. 클립 배치와 실패 복구

```bash
python3 scripts/ingest.py scenes.json
python3 scripts/ingest.py scenes.json --apply
python3 scripts/check.py scenes.json
```

- `없음`: 해당 씬을 다시 내려받거나 경로를 확인한다.
- `짧음`: 더 긴 클립을 다시 만들며, 억지로 늘리지 않는다.
- 순서 오류: `--map 씬번호=파일경로`로 한 씬만 지정한다.
- 원본은 지우지 않는다. 배치는 복사 방식으로 한다.

### D. Typecast TTS와 조립

API를 쓰는 사람만 자신의 컴퓨터 터미널 환경변수에 키를 넣는다. 아래 첫 줄은 예시이므로 강의 화면이나 오픈카톡에 실제 키를 붙여넣지 않는다. 키 설정은 운영자 안내를 받은 뒤 진행한다.

```bash
export TYPECAST_API_KEY='본인 키'
python3 scripts/tts.py script.txt work/ --voice 본인_voice_id
python3 scripts/build.py scenes.json work/ --out D7_닉네임_쇼츠.mp4
```

현재 개발본은 `narration.wav`와 단어별 `words.json`을 만든다. `build.py`는 그 타이밍으로 자막을 합성한다.

## 보안·비용·저작권

- API 키는 채팅, 화면 캡처, `script.txt`, `prompts.md`, Git 저장소에 넣지 않는다.
- `.env`를 사용한다면 Git 제외 여부를 먼저 확인한다. 이 개발본에는 `.env` 처리 코드가 없으므로 키는 터미널 환경변수로만 전달한다.
- Flow와 Typecast의 이용 가능 여부, 크레딧, 요금제, 상업 이용 조건은 실행 당일 각 서비스의 공식 화면에서 확인한다.
- 현재 개발 문서는 Typecast 무료 다운로드분의 상업 이용 불가와 웹/API 플랜 분리를 경고한다. 운영자는 배포 전에 공식 약관과 현재 계정 화면으로 다시 확인해야 한다.
- 수강생이 직접 촬영했거나 생성·사용 권한을 확인한 영상·사진·음성만 공개한다.
- 유튜브 레퍼런스는 구조 분석용이다. 영상, 대본, 음악, 썸네일을 그대로 복제하지 않는다.

## 막혔을 때 Claude에게 보내는 복구 프롬프트

```text
방금 실행한 명령과 오류는 아래와 같아.
[명령 붙여넣기]
[오류 붙여넣기]

원본과 이미 완성된 파일을 삭제하거나 덮어쓰지 마.
API 키 값은 출력하지 마.
가능한 원인을 3개 이내로 좁히고, 가장 안전한 읽기 전용 확인부터 한 단계씩 진행해 줘.
각 단계가 끝날 때 내가 확인할 결과도 알려 줘.
```
