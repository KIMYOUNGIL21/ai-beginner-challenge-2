# Day 5 — Claude Code로 눌러서 쓰는 웹 도구 만들기

오늘은 Claude Desktop의 **Code**를 처음 제대로 사용합니다. 코드를 외우지 않습니다. 안전한 폴더를 열고, 만들 파일을 먼저 확인하고, 변경 내용을 승인하고, Preview에서 버튼을 눌러 봅니다.

> **Cowork와 Code의 차이:** 어제 Cowork는 자료를 읽어 문서로 정리했습니다. 오늘 Code는 폴더 안의 HTML 파일을 만들고, 화면과 기능을 직접 고칩니다.

## 이 기능이 무엇인가요?

**Claude Code**는 Claude가 내가 고른 프로젝트 폴더의 파일을 읽고, 만들고, 고치도록 부탁하는 기능입니다. Desktop의 Code 탭에서는 까만 터미널을 먼저 배우지 않아도 파일 목록, 변경 전후인 Diff, 실제 화면인 Preview를 눈으로 보며 작업할 수 있습니다.

오늘은 코딩 문법을 배우는 날이 아닙니다. “어느 폴더를 열었는지 → 어떤 파일을 만들지 → 변경을 승인할지 → 실제로 작동하는지”를 사람이 판단하는 법을 배웁니다.

## Claude Desktop 화면에서 어디에 있나요?

Claude Desktop의 왼쪽 또는 홈 화면에서 **Code**를 누르고, 내 컴퓨터에서 작업하는 **Local** 세션을 선택합니다.

![Claude Desktop Code의 Local과 폴더 선택 실제 화면](day05-assets/screenshots/01_code_local.png)

이 화면에서 다음 순서로 누릅니다.

1. Claude Desktop 위쪽 또는 왼쪽의 **Code**를 누릅니다.
2. 화면 왼쪽 위의 **Local(로컬)**을 누릅니다.
3. 바로 옆 **폴더 선택…**을 누르고 오늘 폴더를 엽니다.
4. 입력창 아래 왼쪽 권한 메뉴를 **Manual(수동)**로 바꿉니다.
5. 오른쪽 모델 메뉴의 Effort를 **High(높음)**로 둡니다.

Code의 **Manual(수동)**은 공식 문서와 이전 앱에서 `Ask permissions`로 불리던 초보자 권장 방식입니다. 파일을 고치거나 명령을 실행하기 전에 묻습니다. 어제 Cowork의 Manual과 이름은 같지만, 오늘은 **Code 프로젝트 파일과 명령**에 적용됩니다.

Effort의 공식 단계는 모델에 따라 Low, Medium, High, Extra high(xhigh), Max입니다. 수강 기본값은 **보이는 선택지 중 High**입니다. 단순 복사·요약은 Medium, 여러 파일을 만들고 검수하는 작업은 High 이상이 알맞습니다. `Ultra`가 별도로 보여도 이번 기본값은 아닙니다.

## 오늘 쌓이는 결과물

![Day 5 브라우저에서 실제로 작동하는 웹 도구 완성 예시](day05-assets/expected/code-tool-example.png)

[완성 예시 웹 도구 직접 눌러 보기](day05-assets/완성예시/index.html)

1. 작은 결과물: 큰 글씨로 내 문장을 보여 주는 `hello.html`
2. 메인 결과물: 입력칸과 버튼이 작동하는 `index.html`
3. 자기 주제 응용본: 제목·대상·색·안내 문장을 바꾼 나만의 웹 도구
4. 다음 날 연결 파일: 프로젝트의 대상·목표·말투·안전 규칙을 담은 `CLAUDE.md`
5. 오픈카톡 인증: HTML 파일과 작동 화면 캡처

## 예상 시간

| 구간 | 왕초보 예상 시간 | 완료 표시 |
|---|---:|---|
| Code 화면과 권한 이해 | 30분 | Manual(수동)이 보임 |
| 작은 결과물 | 35분 | hello.html Preview가 열림 |
| 메인 결과물 | 70분 | 입력과 버튼이 작동함 |
| 자기 주제 응용 | 60분 | 세 가지가 내 내용으로 바뀜 |
| 다음 날 규칙 저장 | 25분 | `CLAUDE.md`가 생김 |
| 검수·인증 | 30분 | 파일과 캡처를 제출함 |

처음이면 약 4시간 10분을 잡습니다.

## 준비물

- Day 0에서 설치한 Claude Desktop
- [Day 5 시작 자료 ZIP 한 번에 받기](downloads/day05-start.zip)
- 공개해도 되는 도구 이름·대상·예시 한 줄
- 휴대폰

API 키, Terminal, Node, Python, GitHub는 오늘 필요 없습니다.

## 1. 작업용 복사 폴더를 만듭니다

### Mac

1. Finder에서 받은 `day05-assets` 폴더를 찾습니다.
2. 폴더를 한 번 누르고 `Command+D`를 눌러 복제합니다.
3. 복제본 이름을 `Day05_Code_닉네임`으로 바꿉니다.

### Windows

1. 파일 탐색기에서 받은 `day05-assets` 폴더를 찾습니다.
2. 폴더를 마우스 오른쪽으로 눌러 **복사**를 고릅니다.
3. 바탕 화면 빈 곳에서 마우스 오른쪽을 눌러 **붙여넣기**를 고릅니다.
4. 복사본 이름을 `Day05_Code_닉네임`으로 바꿉니다.

폴더 안에 `내정보.txt`와 `완성예시` 폴더가 있으면 성공입니다. 완성예시는 막혔을 때 비교하는 답안입니다.

## 2. Code에서 폴더를 안전하게 엽니다

1. Claude Desktop을 열고 **Code** 탭을 누릅니다.
2. **Local**을 고릅니다.
3. **Select folder / Open folder / 폴더 열기**를 누릅니다.
4. `Day05_Code_닉네임` 폴더를 고릅니다.
5. 입력창 옆 권한 모드를 **Manual(수동, 구 Ask permissions)**로 둡니다.

Manual은 Claude가 파일을 바꾸거나 명령을 실행하기 전에 묻게 하는 초보자 권장 모드입니다. **Bypass permissions는 고르지 않습니다.** 오늘 폴더 밖을 바꾸거나 삭제하는 요청이 보이면 거절합니다.

```text
파일을 바꾸지 말고 지금 열린 폴더의 이름과 보이는 파일만 알려 주세요.
오늘 폴더 밖은 읽거나 수정하지 마세요.
```

**성공 화면:** 답변에 `내정보.txt`가 보입니다.

## 3. 작은 결과물 — 첫 Preview를 엽니다

```text
이 폴더에 hello.html 파일 하나를 만들어 주세요.
화면 가운데에 '닉네임님의 첫 Claude Code 화면'이라는 큰 글자와
'파일을 만들고 Preview로 확인했습니다'라는 문장을 보여 주세요.
외부 이미지, 설치, API, 다른 파일은 사용하지 마세요.
만들기 전에 변경할 파일이 hello.html 하나인지 먼저 알려 주세요.
```

파일 생성 승인 화면에서 경로 끝이 `Day05_Code_닉네임/hello.html`인지 확인합니다. Diff가 보이면 새 파일 하나뿐인지 보고 승인합니다.

1. 파일 목록의 `hello.html`을 누릅니다.
2. **Preview/미리보기**를 누릅니다.
3. 큰 제목과 문장이 보이면 성공입니다.

안 보이면 다음만 보냅니다.

```text
hello.html Preview가 보이지 않습니다. 파일은 삭제하지 말고 원인을 확인해 주세요.
제가 다음에 누를 버튼 하나만 쉬운 말로 알려 주세요.
```

## 4. 메인 결과물 — 버튼 웹 도구를 만듭니다

먼저 `내정보.txt`를 열어 다섯 줄을 내 내용으로 바꿉니다. 글자만 바꾸고 항목 이름은 지우지 않습니다. 저장은 Mac `Command+S`, Windows `Ctrl+S`입니다.

그다음 아래 프롬프트를 보냅니다.

```text
@내정보.txt를 읽고 index.html 파일 하나로 작동하는 휴대폰용 웹 도구를 만들어 주세요.

기능:
1. 사용자가 오늘 할 일을 한 줄 입력합니다.
2. '첫 행동 정하기' 버튼을 누르면 입력한 문장을 포함한 5분 시작 문구를 보여 줍니다.
3. 입력이 비어 있으면 먼저 할 일을 적으라는 쉬운 안내를 보여 줍니다.

완성 기준:
- 설치, API 키, 인터넷 연결 없이 index.html 하나만으로 작동합니다.
- 휴대폰에서 제목, 입력칸, 버튼이 크게 보입니다.
- 내정보.txt의 도구 이름, 사용자, 안내 문장, 기본 예시, 주색을 반영합니다.
- 기존 hello.html, 내정보.txt, 완성예시 폴더는 수정하거나 삭제하지 않습니다.
- 먼저 만들 화면과 바꿀 파일을 세 줄로 설명하고 내 확인을 기다립니다.
- 내가 승인하면 index.html을 만들고 Preview에서 직접 기능을 점검합니다.
```

계획에 `index.html 하나 생성`이 보이면 `진행해 주세요.`라고 답합니다. 승인 요청마다 오늘 폴더 안인지 확인합니다.

## 5. 직접 눌러 검수합니다

`index.html`을 Preview로 열고 아래 순서로 누릅니다.

1. 입력칸의 글자를 모두 지웁니다.
2. 버튼을 눌러 “먼저 할 일을 적어 주세요”와 비슷한 안내가 나오는지 봅니다.
3. `블로그 첫 문장 쓰기`를 입력합니다.
4. 버튼을 눌러 결과에 `블로그 첫 문장 쓰기`가 들어가는지 봅니다.
5. Preview 폭을 좁혀도 버튼이 화면 밖으로 나가지 않는지 봅니다.

**성공 화면:** 입력한 일이 결과 상자에 나타나고 빈 입력 안내도 작동합니다.

작동하지 않으면 오류를 일부러 만들거나 코드를 직접 고치지 않습니다.

```text
index.html Preview에서 [내가 누른 것]을 했는데 [보이는 결과]가 나옵니다.
기존 파일을 삭제하지 말고 원인을 확인한 뒤 최소한만 고쳐 주세요.
수정 전 무엇을 바꿀지 쉬운 말로 알려 주고, 수정 뒤 같은 동작을 다시 확인해 주세요.
```

## 6. 내 것으로 세 번 바꿉니다

아래 중 세 개를 고르고 **한 번에 하나씩** 요청합니다.

```text
제목을 [나의 제목]으로 바꾸고 누구를 위한 도구인지 제목 아래에 한 줄로 보여 주세요.
```

```text
주색을 [초록색/보라색/주황색]으로 바꾸되 글자가 잘 읽히는 대비를 유지해 주세요.
```

```text
결과 문장을 제 말투인 [짧고 다정한 말투]로 바꾸되 입력한 할 일은 그대로 포함해 주세요.
```

```text
'다시 쓰기' 버튼을 추가해 입력과 결과를 비우게 하고 직접 눌러 확인해 주세요.
```

매번 Diff에서 예상한 파일이 `index.html` 하나인지 보고 승인한 뒤 Preview를 새로고침합니다.

## 7. 일반 브라우저와 휴대폰에서 확인합니다

1. Finder 또는 파일 탐색기에서 오늘 폴더를 엽니다.
2. `index.html`을 두 번 눌러 Chrome, Edge 또는 Safari에서 엽니다.
3. 입력과 버튼을 다시 시험합니다.
4. `index.html`을 카카오톡 **나와의 채팅**, AirDrop 또는 본인이 쓰는 Drive로 휴대폰에 보냅니다.
5. 휴대폰에서 HTML 직접 열기가 막히면 컴퓨터 브라우저 창 폭을 휴대폰처럼 좁혀 결과 화면을 캡처합니다. HTML 원본은 운영자에게 별도로 제출합니다.

> HTML 파일은 카카오톡 안에서 앱처럼 바로 실행되지 않을 수 있습니다. 그래서 기본 인증은 HTML 원본과 작동 화면 캡처입니다. 공개 URL 만들기는 별도 배포 수업에서 진행합니다.

## 8. 다음 날에도 규칙을 기억할 `CLAUDE.md`를 만듭니다

`CLAUDE.md`는 이 프로젝트에서 새 Code 세션을 열어도 Claude가 계속 참고할 작업 규칙입니다. 아래 프롬프트를 보냅니다.

```text
@내정보.txt와 현재 index.html을 읽고 프로젝트 루트에 CLAUDE.md를 만들어 주세요.

반드시 포함할 것:
1. 이 도구의 이름과 사용할 사람
2. 이 도구가 해결하려는 문제 한 문장
3. 문장은 짧고 쉬운 한국어로 쓴다는 말투 규칙
4. 휴대폰에서 큰 글자와 버튼을 유지한다는 화면 규칙
5. 개인정보, API 키, 확인되지 않은 사실을 넣지 않는다는 안전 규칙
6. 기존 index.html을 삭제하지 않고 변경 전에 계획을 보여 준다는 작업 규칙

내정보.txt에 없는 취향이나 사실은 만들지 마세요.
먼저 들어갈 내용을 여섯 줄로 보여 주고, 내가 승인하면 CLAUDE.md 하나만 새로 만드세요.
```

승인 창에서 오늘 폴더 바로 아래의 `CLAUDE.md` 하나만 추가되는지 확인합니다. 파일을 열어 내 도구 이름과 대상이 정확한지 읽습니다.

**성공 화면:** 파일 목록에 `CLAUDE.md`가 있고, 위 여섯 항목이 쉬운 말로 적혀 있습니다. 완성 예시는 `day05-assets/완성예시/CLAUDE.md`에서 비교할 수 있습니다.

## 9. 오픈카톡 인증

```text
[왕초보2기 Day 5 / 닉네임]
오늘 만든 것: 입력하고 버튼을 누르면 첫 행동을 보여 주는 웹 도구
누구를 위한 것: ___
직접 확인한 기능: 빈 입력 안내 / 입력 결과 / 다시 쓰기(선택)
내가 바꾼 세 가지: ___ / ___ / ___
오늘 배운 것: Code에서 파일 변경을 승인하고 Preview로 기능을 확인하는 법
다음 날 준비: CLAUDE.md 생성 완료
```

함께 올릴 것:

- `D05_닉네임_Code웹도구.png` 화면 1장
- `index.html` 원본 또는 운영자가 지정한 제출 링크

## 최종 합격표

- [ ] Manual(수동)에서 오늘 폴더만 열었다.
- [ ] `hello.html`과 `index.html`이 실제로 존재한다.
- [ ] 프로젝트 루트에 `CLAUDE.md`가 있고 도구의 대상·목표·말투·화면·안전 규칙이 있다.
- [ ] 빈 입력 안내와 입력 결과가 모두 작동한다.
- [ ] 제목·색·문장 등 세 가지가 내 내용이다.
- [ ] 일반 브라우저에서도 작동한다.
- [ ] 개인정보·API 키·남의 저작물이 없다.
- [ ] 다른 사람이 볼 수 있는 화면 캡처와 HTML 원본이 있다.

## 자주 막히는 화면

| 상황 | 가장 쉬운 복구 |
|---|---|
| Code 또는 Local이 안 보임 | 앱을 업데이트하고 로그인·유료 요금제를 확인한 뒤 다시 엽니다. |
| 권한 창의 경로가 낯섦 | 거절하고 현재 폴더 이름이 `Day05_Code_닉네임`인지 다시 봅니다. |
| Preview가 하얗게 보임 | Preview를 닫고 파일 목록에서 index.html을 다시 선택합니다. 계속되면 복구 프롬프트를 보냅니다. |
| 수정했는데 옛 화면임 | Preview의 새로고침을 누르거나 닫았다 다시 엽니다. |
| HTML이 휴대폰에서 안 열림 | 컴퓨터의 작동 화면 캡처+HTML 원본으로 제출합니다. |
| 삭제 또는 설치 명령 승인 요청 | 거절합니다. 오늘 과제에는 삭제와 설치가 필요 없습니다. |

## 공식 레퍼런스와 시연

- [Claude Code Desktop 공식 가이드](https://code.claude.com/docs/en/desktop)
- [Claude Desktop 다운로드](https://claude.com/download)
- [Cowork와 Code가 포함된 Desktop 공식 설명](https://claude.com/blog/the-claude-cowork-product-guide)

공식 가이드의 초보자 권장 방식인 **Manual(구 Ask permissions)**, 시각적 Diff 검토, 파일 보기, Preview를 오늘 순서에 그대로 반영했습니다.

## 운영자 캡처 체크리스트

- `day05-assets/screenshots/01_code_local.png`: 최신 앱에서 Code와 Local을 고르는 첫 화면
- 현재 교재에는 실제 내용이 확인된 위 PNG 한 장만 연결했습니다. 존재하지 않거나 빈 캡처 링크는 넣지 않습니다.
