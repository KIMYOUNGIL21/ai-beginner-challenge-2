# Day 5 — 입력만 바꾸면 다시 만들어지는 미니 자동화

## 오늘의 목표

Day 4 카드뉴스를 매번 처음부터 만들지 않도록, **입력 파일 1개를 수정하고 명령 1개를 실행하면 새 결과가 나오는 자동화**로 바꾼다. Terminal을 외우는 날이 아니라, 실행→오류 읽기→수정→재실행의 흐름을 경험하는 날이다.

처음 보는 말:

- **Terminal(터미널)**: 컴퓨터에 글자로 명령을 내리는 창이다. 오늘은 한 줄을 복사해 붙여넣기만 한다.
- **명령**: 터미널에 붙여넣고 Enter를 누르는 한 줄이다.
- **JSON**: 제목·색·문구 같은 입력값을 정해진 모양으로 적는 파일이다. 큰따옴표와 쉼표를 함부로 지우지 않는다.
- **스크립트**: 반복 작업을 대신 실행하도록 적어 둔 작은 프로그램이다.
- **API 키**: 외부 서비스 사용자를 확인하는 비밀 문자열이다. 오늘은 필요 없다.

## 오늘의 인증물

- 입력 A와 입력 B로 각각 만든 결과 화면
- 입력 변경부터 새 결과가 나타날 때까지 담은 30초 이내 화면 녹화 `D05_닉네임_자동화시연.mp4`
- 실제 실행 명령 한 줄

영상에는 비밀번호, 키, 개인 폴더, 다른 수강생 정보가 보이면 안 된다.

### 기본 완주와 심화

**기본 완주**는 터미널에 한 줄을 복사해 입력 A와 입력 B의 결과가 각각 만들어지는 것까지다. 코드를 읽거나 직접 쓸 필요는 없다.

**심화**는 `run.command`를 만들어 더블클릭으로 실행하고, 잘못된 입력을 한국어로 알려 주는 검사까지 추가하는 것이다.

## 4블록 진행

### 1. 보기 — 20분

Day 4 폴더를 열고 다음을 구분한다.

- **입력**: 주제, 대상, 카드 문구, 색
- **규칙**: 5장 구성, 크기, 여백, 파일명
- **출력**: SVG 5장과 검수용 HTML

자동화는 “Claude에게 같은 말을 다시 하는 것”이 아니라 입력과 규칙을 파일에 저장하고 같은 명령으로 반복하는 것이다.

### 2. 따라하기 — 60분

Day 4 폴더를 복사해 `day05-automation`으로 이름을 바꾼다. 완성 뒤 구조는 다음과 같아야 한다.

```text
day05-automation/
├── input/
│   └── content.json
├── scripts/
│   └── build.mjs       # 환경에 따라 build.py 가능
├── output/
│   ├── card01.svg ... card05.svg
│   └── index.html
├── README.md
└── run.command         # 선택, Mac에서 더블클릭 실행
```

완성형 프롬프트를 실행한다. Claude가 먼저 `node --version` 또는 `python3 --version`을 확인하게 한다. 이 명령은 각각 Node와 Python이 설치되어 있는지 확인할 뿐이며 파일을 바꾸지 않는다. 추가 프로그램을 설치하지 않고 현재 Mac에서 되는 방식을 고른다.

### 3. 내 것으로 바꾸기 — 60분

1. 입력 A로 한 번 성공한다.
2. `input/content.json`에서 주제, 대상, 주색, 다섯 장 문구를 입력 B로 바꾼다.
3. **같은 명령**을 다시 실행한다.
4. 출력 파일의 수정 시간이 바뀌고 내용이 입력 B로 달라졌는지 확인한다.

코드는 이해하지 못해도 된다. 다만 “어느 파일이 입력이고, 어떤 명령을 실행하며, 결과가 어디에 생기는지”는 본인이 말할 수 있어야 한다.

### 4. 검수와 인증 — 30분

1. 화면 녹화를 켠다.
2. `content.json`의 제목 한 줄과 색 값을 바꾼다.
3. Terminal에서 실행 명령을 입력한다.
4. Preview를 새로고침해 새 결과를 보여 준다.
5. 30초 이내로 잘라 인증한다.

## 준비물

- 정상적으로 열리는 Day 4 카드뉴스 폴더
- 입력 A와 확실히 다른 새 주제 1개
- Claude Code Desktop의 Code 탭과 통합 Terminal
- 화면 녹화 기능(기본 QuickTime 또는 macOS 화면 기록)

## 완성형 프롬프트

```text
Day 4 카드뉴스를 입력 파일 하나로 재생산하는 초보자용 자동화로 바꿔 주세요.

조건:
1. 먼저 이 폴더를 읽고 현재 입력, 디자인 규칙, 출력을 구분해 설명합니다.
2. Terminal에서 node --version과 python3 --version을 확인하고, 설치 없이 실행 가능한 쪽을 하나만 고릅니다.
3. input/content.json 한 파일에 brand, audience, colors, cards 5개의 title/body를 저장합니다.
4. 명령 한 줄로 output/card01.svg~card05.svg와 output/index.html을 새로 만듭니다.
5. 외부 패키지와 API 키를 사용하지 않습니다.
6. JSON 문법 오류, 카드 개수 오류, 빈 제목이 있으면 한국어로 무엇을 고칠지 알려 주고 중단합니다.
7. 기존 output은 입력 검증이 성공한 뒤에만 덮어씁니다.
8. README.md에 '입력 수정 → 명령 실행 → 결과 확인' 세 단계와 실제 명령을 적습니다.
9. 가능하면 같은 명령을 실행하는 run.command도 만들되, 작동하지 않아도 Terminal 명령으로 완주할 수 있게 합니다.

작업 후:
- 샘플 입력으로 직접 실행합니다.
- output 파일 6개가 생겼는지 확인합니다.
- 입력 제목을 TEST로 바꿔 재실행하고 결과에 TEST가 들어가는지 검사한 뒤 원래 샘플로 돌려놓습니다.
- 내가 화면 녹화할 정확한 30초 동선을 알려 주세요.
```

## 구체 조작 순서

1. Claude Code Desktop에서 `day05-automation` 폴더를 연다.
2. 완성형 프롬프트를 실행하고 Terminal 명령 권한 요청을 읽은 뒤 허용한다.
3. **Diff**에서 삭제된 파일이 없는지, 키나 개인정보가 새로 기록되지 않았는지 확인한다.
4. `README.md` 맨 위의 실행 명령을 **한 줄 전체** 복사한다. 아래 둘 중 프로젝트에 적힌 한 가지만 사용한다.

```bash
node scripts/build.mjs
```

또는

```bash
python3 scripts/build.py
```

5. 통합 **Terminal**을 열고 명령을 붙여넣어 Enter를 누른다.
6. `output/index.html`을 **Preview**에서 연다.
7. `input/content.json`의 문자열은 큰따옴표 안쪽만 바꾼다. 줄 끝 쉼표를 임의로 지우지 않는다.
8. 같은 명령을 재실행하고 Preview를 새로고침한다.
9. 성공했다면 화면 녹화를 한 번 더 찍는다.

## 성공 판정

- [ ] 실행 명령이 한 줄이며 README에 적혀 있다.
- [ ] 입력 파일과 출력 폴더가 분리되어 있다.
- [ ] 입력 A와 입력 B가 같은 명령으로 서로 다른 결과를 만든다.
- [ ] 잘못된 입력은 출력물을 망가뜨리기 전에 한국어 오류를 보여 준다.
- [ ] 인터넷과 API 키 없이 실행된다.
- [ ] 영상에서 입력 변경, 명령 실행, 새 결과를 모두 확인할 수 있다.

## 막혔을 때 복구

| 화면의 말/증상 | 뜻 | 복구 방법 |
|---|---|---|
| `command not found: node` | Node가 없음 | `python3 --version`을 확인하고 Claude에게 Python 버전으로 바꿔 달라고 한다. 둘 다 없으면 운영자에게 기준 실행환경 설치를 요청한다. |
| `JSON.parse` 또는 `Unexpected token` | 입력 파일의 쉼표·따옴표 오류 | `input/content.json의 문법만 검사하고 내용은 바꾸지 말고 고쳐 주세요.` |
| `No such file or directory` | 현재 폴더 또는 경로가 다름 | Terminal의 현재 위치를 확인해 달라고 하고, 프로젝트 루트에서 실행한다. |
| 실행은 됐지만 옛 화면 | Preview 캐시 또는 새로고침 문제 | 출력 파일 수정 시간을 확인하고 Preview를 닫았다 다시 연다. |
| 카드가 4장만 생성됨 | 입력 배열 개수 오류 | `cards는 정확히 5개여야 한다는 검사를 추가하고 빠진 번호를 알려 주세요.` |
| 원본이 사라짐 | 출력 전 검증/백업 부족 | 추가 실행을 멈추고 Diff와 휴지통을 확인한다. 운영자에게 복구를 요청한다. |

오류 문장을 통째로 Claude 채팅에 붙이고 “원인을 한 문장으로 설명한 뒤 최소 변경으로 고쳐 줘”라고 요청한다.

## 공개·보안 확인

- API 키는 오늘 자동화에 필요 없다. 키를 요구하는 코드가 생기면 실행을 멈춘다.
- Terminal 전체 화면 대신 프로젝트 폴더와 실행 한 줄만 녹화한다.
- `content.json`에는 고객 실명, 연락처, 미공개 매출 자료를 넣지 않는다.
- 모르는 설치 명령, 특히 관리자 비밀번호를 요구하는 명령은 운영자 확인 전 실행하지 않는다.
- 결과 폴더만 공유한다. 개인 홈 폴더 전체나 `.env` 파일은 올리지 않는다.

## 오픈카톡 인증 예시

```text
[왕초보2기 Day 5 / 고래]
오늘 만든 것: 입력 파일만 바꾸면 카드뉴스 5장을 다시 만드는 자동화
누구를 위한 것: 매일 교육 콘텐츠를 만드는 1인 강사
결과물: D05_고래_자동화시연.mp4 + 새 카드 대표 PNG
실행 명령: node scripts/build.mjs
오늘 배운 것: 입력과 규칙을 분리해 같은 명령으로 새 결과를 만들 수 있게 됐습니다.
다음에 고칠 것: 긴 문장을 자동으로 경고하도록 만들겠습니다.
```
